---
import type { Lang } from '~/i18n/strings'
import { t } from '~/i18n/strings'
import { pathFor } from '~/i18n/utils'
import Icon from '~/components/Icon.astro'

interface Props {
  lang: Lang
  topOnly?: boolean
}

const { lang, topOnly = false } = Astro.props
const strings = t(lang)
const items = topOnly ? strings.faq.items.slice(0, 3) : strings.faq.items
const faqPath = pathFor(lang, '/faq')
---

<section id="faq" class="faq">
  <div class="container-wide">
    <header class="section-head reveal">
      <span class="chip">FAQ</span>
      <h2 class="section-title">{strings.faq.title}</h2>
      <p class="section-subtitle">{strings.faq.subtitle}</p>
    </header>

    <div class="faq-list">
      {items.map((item, i) => (
        <details class="faq-item reveal" data-testid={`faq-item-${i}`} style={`animation-delay:${i * 50}ms`}>
          <summary class="faq-summary">
            <span class="faq-num" aria-hidden="true">{String(i + 1).padStart(2, '0')}</span>
            <span class="faq-q">{item.q}</span>
            <span class="faq-chevron" aria-hidden="true">
              <Icon name="chevron-down" size={16} />
            </span>
          </summary>
          <div class="faq-answer">
            <p>{item.a}</p>
          </div>
        </details>
      ))}
    </div>

    {topOnly && (
      <div class="see-all-wrap">
        <a href={faqPath} class="see-all-link" data-testid="faq-see-all">
          {strings.faq.seeAll}
          <svg viewBox="0 0 16 16" width="12" height="12" fill="none" stroke="currentColor" stroke-width="1.75" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
            <path d="M3 8h10M9 4l4 4-4 4"/>
          </svg>
        </a>
      </div>
    )}
  </div>
</section>

<style>
  .faq {
    padding-block: clamp(72px, 9vw, 112px);
    background: var(--color-bg);
  }

  .section-head {
    display: flex;
    flex-direction: column;
    gap: 14px;
    margin-bottom: 56px;
    max-width: 44rem;
  }

  .faq-list {
    display: flex;
    flex-direction: column;
    gap: 0;
    max-width: 60rem;
    border: 1px solid var(--color-border);
    border-radius: var(--radius-2xl);
    overflow: hidden;
    background: var(--color-surface);
  }

  .faq-item {
    border-bottom: 1px solid var(--color-border);
    transition: background 200ms var(--ease-out-soft);
  }

  .faq-item:last-child { border-bottom: none; }

  .faq-item[open] {
    background: var(--color-raised);
  }

  .faq-summary {
    display: flex;
    align-items: center;
    gap: 16px;
    padding: 20px 24px;
    cursor: pointer;
    list-style: none;
    user-select: none;
    transition: background 200ms var(--ease-out-soft);
  }

  .faq-summary::-webkit-details-marker { display: none; }

  .faq-summary:hover {
    background: var(--color-raised);
  }

  .faq-summary:focus-visible {
    outline: 2px solid var(--color-accent);
    outline-offset: -2px;
  }

  .faq-num {
    font-family: var(--font-mono);
    font-size: 11px;
    font-weight: 700;
    color: var(--color-accent);
    letter-spacing: 0.06em;
    flex-shrink: 0;
    width: 24px;
  }

  .faq-q {
    font-size: 15px;
    font-weight: 600;
    color: var(--color-text);
    line-height: 1.4;
    flex: 1;
  }

  .faq-chevron {
    flex-shrink: 0;
    color: var(--color-text-muted);
    transition: transform 250ms var(--ease-out-soft), color 200ms var(--ease-out-soft);
  }

  .faq-item[open] .faq-chevron {
    transform: rotate(180deg);
    color: var(--color-accent);
  }

  .faq-answer {
    padding: 0 24px 20px 64px;
  }

  .faq-answer p {
    color: var(--color-text-secondary);
    font-size: 14px;
    line-height: 1.75;
    margin: 0;
  }

  .see-all-wrap {
    margin-top: 24px;
    max-width: 60rem;
  }

  .see-all-link {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    font-size: 13.5px;
    font-weight: 600;
    color: var(--color-accent);
    text-decoration: none;
    transition: opacity 150ms var(--ease-out-soft), gap 150ms var(--ease-out-soft);
  }

  .see-all-link:hover {
    opacity: 0.8;
    gap: 10px;
  }
</style>
